# OLED字库UTF-8到GB2312转换指南

## 已完成的修改

### 1. `oled_font.h` - 头文件修改

已修改结构体和宏定义:

```c
// 原UTF-8格式:
typedef struct {
    const char *text;      // UTF-8编码字符串指针
    const uint8_t data[72];
} ChineseFont24x24_t;

#define OLED_UTF8_TONG   "\xE5\x90\x8C"  // "同" UTF-8编码

// 新GB2312格式:
typedef struct {
    uint16_t code;          // GB2312双字节编码
    const uint8_t data[72];
} ChineseFont24x24_t;

#define OLED_GB2312_TONG   0xCDAC  // "同" GB2312编码
```

### 2. `oled_font.c` - 字库数据修改

已修改所有字库数组:

```c
// 原UTF-8格式:
{OLED_UTF8_TONG, { /* 点阵数据 */ }}

// 新GB2312格式:
{OLED_GB2312_TONG, { /* 点阵数据 */ }}
```

### 3. `oled.c` - 显示函数修改

#### 3.1 修改 `OLED_ShowChinese24x24` 函数

**位置**: 约第325行

**原代码**:
```c
void OLED_ShowChinese24x24(uint8_t x, uint8_t y, const char *text)
{
  // ...
  for (i = 0; i < OLED_Chinese24x24_Count; i++) {
    if (memcmp(OLED_Chinese24x24[i].text, text, 3) == 0) {
      // 显示代码
    }
  }
}
```

**新代码** (参考 `oled_temp.c`中的实现,需手动在oled.c中修改):
```c
void OLED_ShowChinese24x24(uint8_t x, uint8_t y, uint16_t gb_code)
{
  // ...
  for (i = 0; i < OLED_Chinese24x24_Count; i++) {
    if (OLED_Chinese24x24[i].code == gb_code) {
      // 显示代码保持不变
    }
  }
}
```

#### 3.2 修改 `OLED_ShowChinese24x24String` 函数

**位置**: 约第363行

在 `while (*text != '\0')` 循环中:

**原UTF-8解析代码**:
```c
uint8_t lead = (uint8_t)*text;

if ((lead & 0xF0) == 0xE0) {  // UTF-8三字节
  OLED_ShowChinese24x24(current_x, y, text);
  text += 3;
  current_x += 24;
}
```

**新GB2312解析代码** (参考oled_temp.c):
```c
if ((uint8_t)*text >= 0xA1) {  // GB2312首字节
  uint16_t gb_code = ((uint8_t)text[0] << 8) | (uint8_t)text[1];
  OLED_ShowChinese24x24(current_x, y, gb_code);
  text += 2;
  current_x += 24;
} else {
  break;
}
```

#### 3.3 修改 `OLED_ShowChinese16x16` 函数

**位置**: 约第412行

参照24x24的修改方式,将参数从 `const char *text` 改为 `uint16_t gb_code`,并修改查找逻辑。

#### 3.4 修改 `OLED_ShowChinese16x16String` 函数

**位置**: 约第450行

参照24x24String的修改方式,将UTF-8解析改为GB2312解析。

### 4. `oled.h` - 函数声明修改

修改函数声明:

```c
// 原声明:
void OLED_ShowChinese24x24(uint8_t x, uint8_t y, const char *text);
void OLED_ShowChinese16x16(uint8_t x, uint8_t y, const char *text);

// 新声明:
void OLED_ShowChinese24x24(uint8_t x, uint8_t y, uint16_t gb_code);
void OLED_ShowChinese16x16(uint8_t x, uint8_t y, uint16_t gb_code);

// 字符串函数保持const char *参数不变
void OLED_ShowChinese24x24String(uint8_t x, uint8_t y, const char *text);
void OLED_ShowChinese16x16String(uint8_t x, uint8_t y, const char *text);
```

## 使用示例

### 单个汉字显示 (推荐):
```c
OLED_ShowChinese16x16(0, 0, OLED_GB2312_TONG);  // 显示"同"
OLED_ShowChinese16x16(16, 0, OLED_GB2312_SE);   // 显示"色"
```

### 字符串显示:
```c
// 16x16 显示"同色"
const char str_tongse[] = {0xCD, 0xAC, 0xC9, 0xAB, 0x00};  // "同色" + 结束符
OLED_ShowChinese16x16String(0, 0, str_tongse);

// 16x16 显示"异色"  
const char str_yise[] = {0xD2, 0xEC, 0xC9, 0xAB, 0x00};    // "异色" + 结束符
OLED_ShowChinese16x16String(0, 2, str_yise);

// 24x24 显示"同色"
OLED_ShowChinese24x24String(0, 0, str_tongse);
```

## GB2312编码对照表

| 汉字 | GB2312编码 | 宏定义 |
|------|-----------|--------|
| 同   | 0xCDAC    | OLED_GB2312_TONG |
| 异   | 0xD2EC    | OLED_GB2312_YI   |
| 色   | 0xC9AB    | OLED_GB2312_SE   |

## 检查清单

- [x] `oled_font.h` - 修改结构体和宏
- [x] `oled_font.c` - 修改字库数据
- [ ] `oled.h` - 修改函数声明
- [ ] `oled.c` - 修改 OLED_ShowChinese24x24
- [ ] `oled.c` - 修改 OLED_ShowChinese24x24String
- [ ] `oled.c` - 修改 OLED_ShowChinese16x16
- [ ] `oled.c` - 修改 OLED_ShowChinese16x16String

## 完整替换代码

详见 `oled_temp.c` 文件,包含:
1. OLED_ShowChinese24x24String (GB2312版)
2. OLED_ShowChinese16x16 (GB2312版)
3. OLED_ShowChinese16x16String (GB2312版)

请将这些函数复制到 `oled.c` 对应位置替换原函数。
