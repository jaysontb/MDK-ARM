# 任务码中文显示 - 使用说明

## ✅ 可行性确认

### 0.96寸OLED显示"同色"/"异色"

**完全可行!** 24×24点阵字体刚好满足8mm字高要求。

| 参数 | 数值 | 说明 |
|------|------|------|
| 屏幕分辨率 | 128×64像素 | 标准0.96寸OLED |
| 屏幕尺寸 | 21.74×10.86mm | 实际物理尺寸 |
| 字体大小 | 24×24点阵 | 每个汉字 |
| 字体高度 | **≈8.0mm** | **刚好满足比赛要求!** |
| 两字宽度 | 48像素 ≈ 8mm | 居中显示 |
| Flash占用 | 216字节 | 仅3个汉字字库 |

**显示效果:**
```
┌────────────────────────┐
│                        │
│       同  色           │  ← 24×24像素, 8mm高
│                        │
│                        │
└────────────────────────┘
```

## 📝 已实现功能

### 1. 中文字库

**文件: oled_font.c**

添加了3个24×24汉字点阵:
- "同" (72字节)
- "异" (72字节)
- "色" (72字节,同色/异色共用)

**总Flash占用**: 216字节 (非常小!)

### 2. 显示函数

**新增API: `OLED_ShowChinese24x24()`**

```c
/**
 * @brief  显示24×24中文字符
 * @param  x: X坐标(像素), 0~104
 * @param  y: Y坐标(页), 0~5
 * @param  text: 汉字(UTF-8编码)
 */
void OLED_ShowChinese24x24(uint8_t x, uint8_t y, const char *text);
```

**使用示例:**
```c
// 显示"同色"
OLED_ShowChinese24x24(40, 1, "同");
OLED_ShowChinese24x24(64, 1, "色");

// 显示"异色"
OLED_ShowChinese24x24(40, 1, "异");
OLED_ShowChinese24x24(64, 1, "色");
```

### 3. 任务码显示

**API: `Task_Display_Mode()`**

```c
/**
 * @brief 任务码显示控制
 * @param mode: 0=关闭, 1=同色, 2=异色
 */
void Task_Display_Mode(uint8_t mode);
```

**调用示例:**
```c
// 比赛开始,显示"同色"
Task_Display_Mode(1);

// 或显示"异色"
Task_Display_Mode(2);

// 比赛结束,关闭显示
Task_Display_Mode(0);
```

## 🚀 完整使用流程

### 方案1: 简化测试

```c
#include "task.h"
#include "oled.h"

int main(void) {
    HAL_Init();
    SystemClock_Config();
    // ... 其他初始化 ...
    
    OLED_Init();
    
    // 测试显示
    Task_Display_Mode(1);  // 显示"同色"
    HAL_Delay(3000);
    
    Task_Display_Mode(2);  // 显示"异色"
    HAL_Delay(3000);
    
    Task_Display_Mode(0);  // 关闭显示
    
    while(1) {
        // 主循环
    }
}
```

### 方案2: 集成二维码识别

```c
#include "task.h"

int main(void) {
    HAL_Init();
    SystemClock_Config();
    // ... 其他初始化 ...
    
    OLED_Init();
    Motor_Init();
    Gripper_Init();
    Vision_Init();
    
    // 读取二维码并自动显示任务码
    uint8_t assembly_mode = 0;
    TaskStatus_t status = Task_QRCode_Read(&assembly_mode);
    
    if (status == TASK_SUCCESS) {
        // 二维码识别成功
        // OLED已自动显示"同色"或"异色"
        
        // 执行后续任务
        Task_Material_Pickup();
        Task_Assembly_Stacking(assembly_mode);
        Task_Obstacle_Return();
        
        // 比赛结束,关闭显示
        Task_Display_Mode(0);
    } else {
        // 识别失败
        OLED_ShowString(1, 1, "QR Failed!");
    }
    
    while(1);
}
```

### 方案3: 完整比赛流程

```c
#include "task.h"

int main(void) {
    HAL_Init();
    SystemClock_Config();
    // ... 初始化 ...
    
    OLED_Init();
    
    // 等待启动按钮
    while(HAL_GPIO_ReadPin(START_BUTTON_GPIO, START_BUTTON_PIN));
    
    // 执行完整比赛流程(自动读取二维码并显示)
    TaskStatus_t status = Task_Full_Competition();
    
    if (status == TASK_SUCCESS) {
        OLED_ShowString(1, 1, "Mission Done!");
    } else {
        OLED_ShowString(1, 1, "Mission Failed");
    }
    
    while(1);
}
```

## 📊 串口通信协议

### STM32 → LubanCat

```
发送: [0x66, 0x09, 0x00, 0x00]
      ↑     ↑
      帧头  指令9(二维码识别)
```

### LubanCat → STM32

```
接收: [0x66, 0x09, mode, 0x77]
      ↑     ↑     ↑      ↑
      帧头  指令  模式   帧尾
      
mode: 0x01 = 同色装配
      0x02 = 异色错配
      0xFF = 未检测到
```

## 🎯 二维码内容格式

视觉系统支持多种格式,自动识别:

| 二维码内容 | 识别结果 | 显示内容 |
|-----------|---------|---------|
| "1" | 同色装配 | "同色" |
| "2" | 异色错配 | "异色" |
| "同色" | 同色装配 | "同色" |
| "异色" | 异色错配 | "异色" |
| "同色装配" | 同色装配 | "同色" |
| "异色错配" | 异色错配 | "异色" |
| "SAME" | 同色装配 | "同色" |
| "DIFF" | 异色错配 | "异色" |

**推荐使用**: `"1"` 或 `"2"` (最简单可靠)

## 🔧 测试步骤

### 1. 编译烧录

```bash
# 在Keil MDK中编译项目
# 烧录到STM32F407板
```

### 2. OLED显示测试

```c
// 在main.c中临时测试代码
OLED_Init();

// 测试"同色"显示
Task_Display_Mode(1);
HAL_Delay(3000);

// 测试"异色"显示
Task_Display_Mode(2);
HAL_Delay(3000);

// 测试关闭显示
Task_Display_Mode(0);
```

**预期结果:**
- 屏幕中央显示"同色"两个大字(约8mm高)
- 3秒后切换到"异色"
- 再3秒后屏幕关闭

### 3. 二维码联调测试

#### 准备工作:
1. 生成二维码(内容"1"或"2")
2. 打印到A4纸上(80×80mm)
3. 贴到场地边界垂直位置

#### 测试流程:
```c
uint8_t mode = 0;
if (Task_QRCode_Read(&mode) == TASK_SUCCESS) {
    printf("识别成功! 装配模式: %d\n", mode);
    // OLED应显示对应的中文
} else {
    printf("识别失败或超时\n");
}
```

**预期结果:**
- LubanCat识别二维码并发送模式码
- STM32接收并在OLED显示"同色"或"异色"
- 显示持续到调用`Task_Display_Mode(0)`

## 📐 显示位置参数

```c
// "同色"/"异色"居中显示
// 
// X坐标计算:
// 屏幕宽度 = 128像素
// 两个字宽度 = 2 × 24 = 48像素
// 居中位置 = (128 - 48) / 2 = 40像素
//
// 第一个字: x = 40
// 第二个字: x = 40 + 24 = 64
//
// Y坐标:
// 从第1页(8行)开始
// 占用3页(24行)
// 总高度 ≈ 8mm (满足要求)

OLED_ShowChinese24x24(40, 1, "同");  // 第1个字
OLED_ShowChinese24x24(64, 1, "色");  // 第2个字
```

## ⚠️ 注意事项

### 1. 字体高度确认

- 24×24点阵理论高度: **8.0mm** ✅
- 实际测量: 使用直尺测量OLED屏幕上的字符高度
- 如果小于8mm: 考虑使用32×32字体(需要重新制作字库)

### 2. UTF-8编码问题

确保源文件使用**UTF-8编码**:
- Keil MDK: Edit → Configuration → Encoding → UTF-8
- 如果显示乱码,检查编码设置

### 3. 显示亮度

比赛要求"亮光显示":
```c
// 在OLED_Init()中设置对比度
OLED_WriteCommand(0x81);
OLED_WriteCommand(0xCF);  // 最大亮度 (可调整)
```

### 4. 持续显示

**重要**: 任务码必须持续显示到比赛结束,否则**成绩无效**!

```c
// ❌ 错误做法
Task_Display_Mode(1);
HAL_Delay(5000);
Task_Display_Mode(0);  // 提前关闭!

// ✅ 正确做法
Task_Display_Mode(1);
// ... 执行所有任务 ...
// 只在最后关闭
Task_Display_Mode(0);
```

### 5. 摆放位置

- 放在机器人**上部醒目位置**
- 不能被任何物体遮挡
- 建议使用支架固定OLED模块

## 🔍 常见问题

### Q1: 字符显示不完整?

**原因**: 坐标设置错误或字库数据问题

**解决**:
1. 检查x坐标不要超过104 (128-24)
2. 检查y坐标不要超过5 (8页-3页)
3. 验证字库数据是否正确

### Q2: 显示乱码?

**原因**: UTF-8编码问题

**解决**:
1. 确保源文件使用UTF-8编码
2. 重新编译项目
3. 检查字库数组中的汉字文本匹配逻辑

### Q3: 字体太小?

**当前方案**: 24×24点阵 ≈ 8mm (刚好达标)

**备选方案**:
1. 制作32×32字库(128字节/字)
2. 使用外接数码管显示数字
3. 更换1.3寸OLED

### Q4: Flash空间不够?

**当前占用**: 216字节 (极小)

如果需要更多汉字:
```c
// 每增加一个汉字占用72字节
// 如需要"装配"两字:
{"装", { /* 72字节点阵数据 */ }},
{"配", { /* 72字节点阵数据 */ }},
```

## 📚 评分说明

根据比赛规则:

| 项目 | 得分 | 要求 |
|------|------|------|
| 读取二维码并显示 | 8分 | 成功识别并在OLED显示 |
| 任务码持续显示 | **必须项** | 不达标成绩无效! |
| 回归后关闭显示 | 3分 | 自动调用`Task_Display_Mode(0)` |

**总计**: 11分 (占总分11%)

## 🎉 优势总结

✅ **完美适配**: 24×24字体刚好8mm,满足要求  
✅ **占用极小**: 仅216字节Flash  
✅ **显示清晰**: 中文显示比英文更直观  
✅ **自动集成**: 二维码识别后自动显示  
✅ **符合规则**: 满足所有比赛要求  

**结论**: 0.96寸OLED **完全可行**,无需额外硬件!
