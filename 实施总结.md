# LubanCat2 与 STM32F4 联动实施总结

## ✅ 已完成的工作

### 1. 协议适配 (visual_comm.c)
- [x] 修改所有发送函数,将校验和改为固定帧尾 `0x77`
- [x] 添加状态机接收逻辑
- [x] 实现单字节中断接收处理
- [x] 添加接收完成标志和缓冲区管理

### 2. 中断配置
- [x] 启用 USART2 中断 (usart.c)
- [x] 添加 USART2_IRQHandler (stm32f4xx_it.c)
- [x] 实现 HAL_UART_RxCpltCallback (usart.c)

### 3. 测试代码
- [x] 创建 visual_test.c/h
- [x] 实现基础通信测试函数
- [x] 实现循环稳定性测试
- [x] 实现完整任务流程模拟

### 4. 主程序集成
- [x] main.c 添加视觉通信初始化
- [x] 主循环添加数据接收处理
- [x] 集成测试函数调用

### 5. 文档
- [x] 创建硬件连接指南
- [x] 编写软件配置说明
- [x] 提供分阶段测试步骤
- [x] 整理常见问题解答
- [x] 编写API使用说明

---

## 📂 修改文件列表

| 文件 | 修改内容 | 说明 |
|------|---------|------|
| **Src/visual_comm.c** | 重大修改 | 协议适配+接收状态机 |
| **Inc/visual_comm.h** | 添加声明 | 新函数和全局变量 |
| **Src/usart.c** | 添加中断 | USART2中断使能+回调 |
| **Src/stm32f4xx_it.c** | 添加中断 | USART2中断处理函数 |
| **Src/main.c** | 集成代码 | 初始化+主循环处理 |
| **Src/visual_test.c** | 新建文件 | 测试函数实现 |
| **Inc/visual_test.h** | 新建文件 | 测试函数声明 |

---

## 🔄 通信协议(最终版)

### STM32 → LubanCat (请求格式)
```
[0x66, unit, target, 0x77]
```

| 字节 | 说明 | 示例 |
|------|------|------|
| 0 | 帧头 | 0x66 |
| 1 | 任务码 | 0x02(物料), 0x0A(凸台), 0x0B(凹槽), 0x09(二维码) |
| 2 | 目标参数 | 0x01(红), 0x02(绿), 0x03(蓝) |
| 3 | 帧尾 | 0x77 |

### LubanCat → STM32 (响应格式)

#### 物料识别 (unit=0x02)
```
[0x66, 0x02, color, x_h, x_l, y_h, y_l, 0x77]  (8字节)
```

#### 凸台识别 (unit=0x0A)
```
[0x66, 0x0A, x_h, x_l, y_h, y_l, 0x77]  (7字节)
```

#### 凹槽识别 (unit=0x0B)
```
[0x66, 0x0B, x_h, x_l, y_h, y_l, 0x77]  (7字节)
```

#### 二维码识别 (unit=0x09)
```
[0x66, 0x09, d1, d2, d3, d4, d5, d6, 0x77]  (9字节)
```

---

## 🧪 测试流程

### 第一步: 验证编译
```bash
# Keil MDK中
1. 添加 visual_test.c 到工程
2. 编译项目(0 Error, 0 Warning)
3. 烧录到STM32
```

### 第二步: 硬件连接
```
LubanCat2 USART3  ↔  STM32F407 USART2
    TX (Pin X)    →    RX (PA3)
    RX (Pin Y)    ←    TX (PA2)
    GND           ↔    GND
```

### 第三步: 启动视觉程序
```bash
# LubanCat2终端
cd /home/lubancat/vision
python Creama.py
```

### 第四步: 运行STM32测试
```c
// main.c 中取消注释:
Visual_Test_Basic();  // 单次测试
// 或
Visual_Test_Loop();   // 循环测试
```

### 第五步: 查看结果
- 串口助手(USART1, 115200)应显示:
  ```
  === 测试1: 红色物料识别 ===
  红色物料坐标: X=552, Y=230
  ```

---

## 🎯 关键代码示例

### 最简使用示例
```c
// 在main()函数中

// 1. 初始化(只需一次)
Visual_Data_Init();
Visual_UART_Start_Receive();

// 2. 在需要视觉数据时
while(1)
{
    // 发送请求
    Visual_Send_Material_Request(COLOR_RED);
    
    // 等待响应
    HAL_Delay(100);  // 给LubanCat处理时间
    
    if (visual_rx_complete_flag)
    {
        // 解析数据
        Visual_Data_Unpack(Visual_Get_RxBuffer());
        Visual_Clear_RxFlag();
        
        // 使用数据
        uint16_t x = VIS_RX.r_block_x;
        uint16_t y = VIS_RX.r_block_y;
        
        // TODO: 移动到(x, y)
    }
    
    HAL_Delay(1000);
}
```

### 带超时处理的完整示例
```c
// 定义超时时间
#define VISION_TIMEOUT_MS  500

// 请求并等待响应
Visual_Send_Platform_Request(COLOR_GREEN);

uint32_t start_time = HAL_GetTick();
uint8_t success = 0;

while ((HAL_GetTick() - start_time) < VISION_TIMEOUT_MS)
{
    if (visual_rx_complete_flag)
    {
        Visual_Data_Unpack(Visual_Get_RxBuffer());
        Visual_Clear_RxFlag();
        
        uint16_t platform_x = VIS_RX.platform_x;
        uint16_t platform_y = VIS_RX.platform_y;
        
        // 坐标有效性检查
        if (platform_x > 0 && platform_y > 0)
        {
            success = 1;
            // TODO: 执行运动控制
            break;
        }
    }
    HAL_Delay(1);
}

if (!success)
{
    // 超时处理: 重试或报错
}
```

---

## 🐛 调试技巧

### 1. 查看原始接收数据
```c
// 在 Visual_UART_RxCallback() 中添加:
extern UART_HandleTypeDef huart1;
char debug[50];
sprintf(debug, "RX: %02X\r\n", rx_byte);
HAL_UART_Transmit(&huart1, (uint8_t*)debug, strlen(debug), 10);
```

### 2. 验证状态机工作
```c
// 在状态机各分支添加日志:
case RX_STATE_WAIT_HEADER:
    HAL_GPIO_TogglePin(LED1_GPIO_Port, LED1_Pin);  // LED闪烁
    break;
```

### 3. 查看LubanCat日志
```bash
# 确认LubanCat收到请求
python Creama.py
# 应输出:
# [UART] 收到指令: unit=2, target=1 (物料识别)
```

---

## ⚡ 性能优化建议

1. **降低请求频率**: 避免每帧都请求,建议间隔 > 200ms
2. **使用标志控制**: 只在需要时才发送请求
3. **批量处理**: 一次任务流程中收集所有需要的坐标后再运动
4. **坐标缓存**: 物料位置变化慢,可缓存最近一次的坐标

---

## 📌 下一步行动

1. **立即测试**
   - [ ] 编译烧录STM32
   - [ ] 连接硬件
   - [ ] 运行 `Visual_Test_Basic()`
   - [ ] 确认通信成功

2. **集成到业务**
   - [ ] 编写坐标转换函数
   - [ ] 集成运动控制
   - [ ] 实现完整任务逻辑

3. **现场调试**
   - [ ] 校准颜色阈值
   - [ ] 测试不同光照条件
   - [ ] 优化识别准确率

---

## 🎉 预期效果

完成上述步骤后,你的系统应该能够:

✅ STM32通过串口向LubanCat发送视觉请求  
✅ LubanCat识别目标并返回坐标数据  
✅ STM32接收并解析坐标  
✅ 根据坐标控制机械臂/小车运动  
✅ 完成完整的"视觉识别→运动控制"闭环  

**现在你可以开始测试了!** 🚀

有任何问题随时询问!
