# 电机移动模块测试指南

## 📋 测试前准备清单

### 硬件检查
- [ ] 确认4个步进电机已正确接线到Emm_V5驱动器
- [ ] 确认驱动器地址设置正确 (FL=1, FR=2, RL=3, RR=4)
- [ ] 确认电源电压符合要求
- [ ] 确认UART1已连接到驱动器串口
- [ ] 确认OLED显示屏正常工作

### 场地要求
| 测试类型 | 所需空间 | 说明 |
|---------|---------|------|
| 基础测试 | 300×300mm | 前后左右各100mm,旋转90度 |
| 正方形测试 | 500×500mm | 走200mm边长正方形 |
| 比赛路径 | 1500×600mm | 模拟完整比赛路径 |

### 安全措施
1. **急停准备**: 准备好断电开关,随时可以切断电源
2. **防跌落**: 确保小车不会从桌面掉落
3. **观察距离**: 保持1米以上安全距离
4. **首次测试**: 建议先用手托住小车,观察轮子转向

---

## 🚀 测试步骤

### 测试1: 基础运动测试 (推荐首次)

**目的**: 验证前进、后退、平移、旋转基本功能

**操作步骤**:
```c
// 在main.c的USER CODE BEGIN 2区域
Motor_Init();
Motor_Test_Basic();
```

**预期行为**:
1. OLED显示 "Forward 100mm" → 小车前进约100mm → 停止
2. OLED显示 "Back 100mm" → 小车后退约100mm → 停止(回到原位)
3. OLED显示 "Right 50mm" → 小车右移约50mm → 停止
4. OLED显示 "Left 50mm" → 小车左移约50mm → 停止(回到原位)
5. OLED显示 "Rotate 90 CCW" → 小车逆时针转90度 → 停止
6. OLED显示 "Rotate 90 CW" → 小车顺时针转90度 → 停止(回到原方向)

**验收标准**:
- ✅ 所有动作完成后,小车应**基本**回到起点(允许±20mm误差)
- ✅ 小车朝向应与起始方向一致(允许±5度误差)
- ✅ 无异响、抖动、丢步现象

**常见问题**:
| 问题 | 原因 | 解决方案 |
|-----|------|---------|
| 前进变后退 | 电机2/4方向未正确翻转 | 检查`FIX_DIR_MOTOR_2/4`宏定义 |
| 左移变右移 | 麦轮运动学方向错误 | 检查`Motor_Move_Lateral`中的方向设置 |
| 距离误差大 | 轮子半径参数不准 | 测量实际移动距离,调整`WHEEL_RADIUS_MM` |
| 旋转角度不准 | 轮距参数错误 | 测量实际旋转角度,调整`WHEEL_BASE_MM` |

---

### 测试2: 正方形路径测试

**目的**: 检验定位精度和累计误差

**操作步骤**:
```c
// 注释掉Motor_Test_Basic(),改为:
Motor_Test_Square();
```

**预期行为**:
- OLED显示 "Side 1/4" → 前进200mm → 左转90度
- OLED显示 "Side 2/4" → 前进200mm → 左转90度
- OLED显示 "Side 3/4" → 前进200mm → 左转90度
- OLED显示 "Side 4/4" → 前进200mm → 左转90度
- OLED显示 "Square Done! Check Return"

**验收标准**:
- ✅ 小车应回到起点±30mm范围内
- ✅ 四个转角应接近90度
- ✅ 整个路径接近正方形

**误差分析**:
```
如果最终位置偏移方向为:
- 前方偏移 → WHEEL_RADIUS_MM设置过大,减小5-10%
- 后方偏移 → WHEEL_RADIUS_MM设置过小,增大5-10%
- 左侧偏移 → 可能右侧电机速度快,检查电机安装
- 右侧偏移 → 可能左侧电机速度快,检查电机安装

如果最终朝向偏转:
- 逆时针偏转 → WHEEL_BASE_MM/WHEEL_TRACK_MM过大,减小
- 顺时针偏转 → WHEEL_BASE_MM/WHEEL_TRACK_MM过小,增大
```

---

### 测试3: 比赛路径模拟

**目的**: 模拟完整比赛流程

**操作步骤**:
```c
Motor_Test_Competition_Route();
```

**路径说明**:
```
启停区(0,0) 
    ↓ 前进600mm
    → 右移400mm
物料台(600,400) [停留2秒模拟取料]
    ↓ 前进600mm
测试区(1200,400) [停留2秒模拟放料]
    ↓ 转向180度
    ↓ 前进1200mm
    → 左移400mm
    ↓ 转向-180度
启停区(0,0) [到达]
```

**速度策略**:
- 空载移动: 80 RPM (快速)
- 载货移动: 50 RPM (稳定)
- 取放料: 30 RPM (精确)

---

## 🔧 参数调优指南

### 机械参数 (motor.c)

```c
#define WHEEL_RADIUS_MM         48.0f   // 麦轮半径
#define WHEEL_BASE_MM           300.0f  // 左右轮距
#define WHEEL_TRACK_MM          300.0f  // 前后轮距
```

**测量方法**:
1. **轮子半径**: 
   - 让小车前进1000mm
   - 测量实际移动距离 actual_mm
   - 新半径 = 48.0 × 1000.0 / actual_mm

2. **轮距**:
   - 让小车旋转360度(一圈)
   - 如果实际转了不到360度 → 增大轮距参数
   - 如果实际转了超过360度 → 减小轮距参数

### 速度参数 (motor.c)

```c
#define DEFAULT_SPEED_RPM       40U     // 默认速度
#define MAX_SPEED_RPM           120U    // 最大限速
#define DEFAULT_ACCEL           200U    // 加速度

static const uint16_t SPEED_PROFILES[3] = {30, 50, 80};
```

**调整建议**:
- 如果物料容易掉落 → 降低速度和加速度
- 如果时间紧张 → 提高快速模式速度到100RPM
- 如果定位精度要求高 → 降低精确模式速度到20RPM

### 延时参数 (motor.c)

```c
#define EXTRA_DELAY_MS          100U    // 到位后稳定延时
#define POSITION_POLL_INTERVAL  20U     // 位置查询间隔
```

---

## 📊 测试记录表

### 距离测试
| 设定距离(mm) | 实际距离(mm) | 误差(mm) | 误差率 |
|-------------|-------------|---------|--------|
| 100 前进    |             |         |        |
| 100 后退    |             |         |        |
| 50 右移     |             |         |        |
| 50 左移     |             |         |        |

### 角度测试
| 设定角度(度) | 实际角度(度) | 误差(度) |
|-------------|-------------|---------|
| 90 逆时针   |             |         |
| 90 顺时针   |             |         |
| 180 逆时针  |             |         |

### 正方形测试
- 最终位置偏移: X=_____mm, Y=_____mm
- 最终朝向偏转: _____度
- 四个角度平均值: _____度

---

## ⚠️ 故障排查

### 问题1: 小车不动
**可能原因**:
1. 驱动器未上电
2. UART未连接或波特率错误
3. 电机地址设置错误
4. 电机未使能

**排查步骤**:
```c
// 在main.c中添加调试代码
Emm_V5_En_Control(0, true, true);  // 广播使能所有电机
HAL_Delay(100);
```

### 问题2: 运动方向错误
**排查步骤**:
1. 单独测试每个电机:
```c
Emm_V5_Pos_Control(1, 1, 50, 200, 3600, 0, 1);  // 电机1正转360度
HAL_Delay(5000);
```
2. 观察哪个轮子在转,转向是否正确
3. 修改`FIX_DIR_MOTOR_X`宏或接线

### 问题3: 抖动或异响
**可能原因**:
1. 加速度过大 → 降低`DEFAULT_ACCEL`
2. 速度过高 → 降低速度
3. 机械安装问题 → 检查轮子、联轴器

### 问题4: 定位误差大
**原因分析**:
1. 轮子打滑 → 增加轮子摩擦力
2. 参数不准 → 重新测量并调整
3. 地面不平 → 更换测试场地

---

## 📝 下一步计划

完成基础测试后,可以进行:
1. ✅ 集成视觉系统进行位置补偿
2. ✅ 添加MPU6050航向保持功能
3. ✅ 实现里程计位置估算
4. ✅ 开发自动校准程序
5. ✅ 编写完整比赛流程代码

---

## 💡 使用技巧

### 快速测试代码模板
```c
// 在main.c USER CODE BEGIN 2中
Motor_Init();

// 自定义测试序列
Motor_Set_Speed_Profile(1);  // 正常速度
Motor_Move_Forward(100.0f, 0);
HAL_Delay(500);
Motor_Move_Lateral(50.0f, 0);
HAL_Delay(500);
Motor_Move_Rotate(90.0f, 0);
HAL_Delay(500);
```

### 调试输出(通过OLED)
```c
char debug_str[16];
snprintf(debug_str, sizeof(debug_str), "X:%.1f Y:%.1f", x, y);
OLED_ShowString(4, 1, debug_str);
```

---

**祝测试顺利! 🎉**
