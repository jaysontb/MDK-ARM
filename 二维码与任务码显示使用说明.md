# 二维码识别与任务码显示使用说明

## 📋 比赛规则要求

根据**浙江省大学生工程实践与创新能力竞赛**规则:

1. **二维码内容**: 
   - 任务码1 = "同色装配" 
   - 任务码2 = "异色错配"

2. **显示要求**:
   - 字体高度 ≥ 8mm
   - 放置在机器人上部醒目位置
   - 亮光显示,不被遮挡
   - 持续显示至比赛结束
   - 回到启停区后自动关闭显示

## 🔍 0.96寸OLED可行性分析

### 硬件规格
- **分辨率**: 128×64 像素
- **物理尺寸**: 约 21.74mm × 10.86mm
- **字符显示**: 
  - 8×16点阵 ≈ 2.6mm高 (不满足8mm要求)
  - 16×16点阵 ≈ 5.3mm高 (接近但仍不足)
  - **需要使用双倍放大** 才能满足8mm要求

### 显示策略

**方案1: 编号+英文缩写** (当前实现)
```
行1: TASK MODE: 1
行2: SAME COLOR
行3: ASSEMBLY
```

**方案2: 大号数字**(推荐)
```
行1: [空]
行2:    MODE 1     (使用放大字体)
行3:    SAME       (英文说明)
```

**方案3: 中文显示**(需要字库支持)
- 需要添加16×16汉字字库(每个汉字32字节)
- "同色装配"需要128字节Flash空间
- 可以实现但字体仍然偏小

## 💻 代码使用说明

### 1. STM32端调用

```c
// 在main.c或task.c中
#include "task.h"

int main(void) {
    // ... 初始化代码 ...
    
    OLED_Init();
    
    // 比赛开始后读取二维码
    uint8_t assembly_mode = 0;
    TaskStatus_t status = Task_QRCode_Read(&assembly_mode);
    
    if (status == TASK_SUCCESS) {
        // 二维码识别成功,任务码已显示
        // assembly_mode: 0=同色, 1=异色
        
        // 执行后续任务...
        Task_Material_Pickup();
        Task_Assembly_Stacking(assembly_mode);
        
        // 比赛结束,关闭显示
        Task_Display_Mode(0);
    } else {
        // 识别失败处理
        OLED_ShowString(1, 1, "QR Failed!");
    }
}
```

### 2. LubanCat视觉端

二维码内容支持多种格式:
- 纯数字: `"1"` 或 `"2"`
- 中文: `"同色装配"` 或 `"异色错配"`
- 英文: `"SAME"` 或 `"DIFF"`
- 混合: `"1-同色"` 或 `"2-异色"`

**识别逻辑**(Python):
```python
# 智能解析二维码内容
if '1' in data or '同色' in data or 'SAME' in data:
    assembly_mode = 0x01  # 同色装配
elif '2' in data or '异色' in data or 'DIFF' in data:
    assembly_mode = 0x02  # 异色错配
```

**串口发送格式**:
```
[0x66, 0x09, mode, 0x77]
其中 mode: 0x01=同色, 0x02=异色
```

## 🔧 集成到完整流程

```c
TaskStatus_t Task_Full_Competition(void)
{
    TaskStatus_t status;
    uint8_t assembly_mode = 0;
    
    // 阶段0: 读取二维码并显示任务码 (得8分)
    status = Task_QRCode_Read(&assembly_mode);
    if (status != TASK_SUCCESS) {
        return status;
    }
    
    // 阶段1: 物料台抓取 (得12分)
    status = Task_Material_Pickup();
    if (status != TASK_SUCCESS) {
        return status;
    }
    
    // 阶段2: 测试区放置 (得30分)
    status = Task_Test_Zone_Place(COLOR_BLUE);
    status = Task_Test_Zone_Place(COLOR_GREEN);
    status = Task_Test_Zone_Place(COLOR_RED);
    
    // 阶段3: 装配码垛 (使用二维码指示的模式, 得27分)
    status = Task_Assembly_Stacking(assembly_mode);
    
    // 阶段4: 障碍穿越与回归 (得23分)
    status = Task_Obstacle_Return();
    
    // 比赛结束,关闭任务码显示 (得3分)
    Task_Display_Mode(0);
    
    return status;
}
```

## 📊 串口通信时序

```
STM32发送: [0x66, 0x09, 0x00, 0x00]
           ↓
LubanCat:  打开摄像头10 → 扫描二维码 → 解析内容
           ↓
LubanCat返回: [0x66, 0x09, 0x01, 0x77]  (同色装配)
           或 [0x66, 0x09, 0x02, 0x77]  (异色错配)
           ↓
STM32:     接收 → 解析模式 → OLED显示 → 保存模式
```

## ⚠️ 注意事项

### 1. 字体大小问题
- 0.96寸OLED标准字体**无法满足8mm要求**
- 建议方案:
  - ✅ 使用编号替代中文(MODE 1 / MODE 2)
  - ✅ 外加LED数码管显示大号数字
  - ❌ 放弃0.96寸,改用1.3寸或更大OLED

### 2. 二维码制作
- 使用在线工具生成 80×80mm 二维码
- 内容建议: `"1"` 或 `"2"` (简单可靠)
- 打印在白色A4纸上,亚光表面
- 现场安装在场地边界内侧垂直位置

### 3. 摄像头配置
- **摄像头9**: 主摄像头,用于物料/凸台/凹槽识别
- **摄像头10**: 二维码专用摄像头,仅在unit=9时临时打开
- 确保两个摄像头不会同时占用资源

### 4. 超时处理
- 二维码扫描超时时间: 5秒
- 超时后可以:
  - 重试扫描
  - 使用默认模式(同色装配)
  - 显示错误并停止

## 📝 评分说明

根据比赛规则:
- **读取二维码并成功显示**: 8分
- **任务码持续显示至比赛结束**: 必须项(否则成绩无效)
- **回归启停区后关闭显示**: 3分

**注意**: 如果任务码未持续显示或被遮挡,**整场比赛成绩无效**!

## 🔨 可选增强方案

如果0.96寸OLED无法满足要求,可以考虑:

1. **添加7段数码管**
   - 显示大号数字 "1" 或 "2"
   - 字高可达20mm以上
   - 成本低,亮度高

2. **更换大尺寸OLED**
   - 1.3寸OLED (64×128 或 128×64)
   - 1.5寸OLED (128×128)
   - 可以显示8mm以上字体

3. **使用LED点阵屏**
   - 8×8或16×16 LED点阵
   - 可以显示简单图形和数字
   - 亮度极高,醒目

## 📚 相关函数文档

### Task_QRCode_Read()
```c
/**
 * @brief 二维码识别并显示任务码
 * @param assembly_mode 输出参数: 装配模式(0=同色, 1=异色)
 * @return TASK_SUCCESS=成功, TASK_TIMEOUT=超时, TASK_FAILED=失败
 * @note 自动调用Task_Display_Mode()显示任务码
 */
TaskStatus_t Task_QRCode_Read(uint8_t *assembly_mode);
```

### Task_Display_Mode()
```c
/**
 * @brief 任务码显示控制
 * @param mode 0=关闭, 1=同色装配, 2=异色错配
 * @note 显示内容:
 *       mode=1: "TASK MODE: 1\nSAME COLOR\nASSEMBLY"
 *       mode=2: "TASK MODE: 2\nDIFF COLOR\nASSEMBLY"
 */
void Task_Display_Mode(uint8_t mode);
```

## 🎯 测试流程

1. **视觉端测试**:
   ```bash
   cd /path/to/Vision
   python3 Creama.py
   # 手动输入: unit=9, 观察二维码识别
   ```

2. **STM32端测试**:
   ```c
   // 在main.c中
   OLED_Init();
   Task_Display_Mode(1);  // 测试同色显示
   HAL_Delay(3000);
   Task_Display_Mode(2);  // 测试异色显示
   HAL_Delay(3000);
   Task_Display_Mode(0);  // 测试关闭
   ```

3. **联调测试**:
   - 准备二维码(内容"1"或"2")
   - 启动LubanCat视觉程序
   - STM32调用`Task_QRCode_Read()`
   - 观察OLED显示是否正确

## 📞 常见问题

**Q: OLED字体太小怎么办?**
A: 可以添加外部LED数码管,或更换更大尺寸的OLED屏幕。

**Q: 串口收不到二维码数据?**
A: 检查波特率(115200)、摄像头索引(10)、二维码内容格式。

**Q: 任务码显示后可以修改吗?**
A: 可以,调用`Task_Display_Mode()`即可切换,但比赛中不应修改。

**Q: 比赛结束忘记关闭显示会扣分吗?**
A: 会扣3分(收藏项),建议在`Task_Obstacle_Return()`末尾自动调用`Task_Display_Mode(0)`。
