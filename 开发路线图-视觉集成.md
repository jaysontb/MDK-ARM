# 🎯 视觉集成开发路线图

## ✅ 已完成部分
- [x] 机械爪升降控制 (Gripper_Lift)
- [x] 云台旋转控制 (Gripper_Pan_Rotate)
- [x] 爪子开合控制 (Gripper_Claw_Open/Close)
- [x] 抓取放置测试 (Test_Gripper_PickAndPlace)

---

## 🚀 接下来要做的 (分4个阶段)

### 📍 **阶段1: 视觉通信验证** (预计1-2天)

#### 1.1 启用视觉模块
在 `main.c` 中取消注释:
```c
Visual_Data_Init();          // 初始化视觉数据结构
Visual_UART_Start_Receive(); // 启动UART3接收
```

#### 1.2 测试视觉通信
运行测试用例:
```c
Test_Vision_Communication();  // 基础通信测试
```

**验证点:**
- [ ] STM32能成功发送请求到LubanCat
- [ ] UART3能正确接收LubanCat返回的数据
- [ ] 数据帧校验通过 (`Visual_Verify_Frame`)

#### 1.3 调试建议
如果通信失败,检查:
1. UART3波特率是否匹配 (默认115200)
2. LubanCat上的视觉服务是否启动 (`vision_service.sh`)
3. 串口接线是否正确 (TX3→RX, RX3→TX, GND)

---

### 📍 **阶段2: 单物料视觉识别** (预计2-3天)

#### 2.1 标定相机坐标系
1. 在物料台固定位置放置红色物料
2. 运行 `Test_Vision_Positioning()` 测试
3. 记录实际移动距离与预期坐标的偏差
4. 调整转换系数:
```c
// 在 task_vision_gripper.c 中修改:
float move_x_mm = (target_x - 320) * K_X;  // 调整K_X
float move_y_mm = (240 - target_y) * K_Y;  // 调整K_Y
```

#### 2.2 测试单物料抓取
运行测试用例:
```c
Test_Vision_Pickup_Red();  // 仅抓取红色物料
```

**验证点:**
- [ ] 视觉能准确识别红色物料位置
- [ ] 底盘能移动到物料正上方 (误差<5mm)
- [ ] 机械爪能成功夹紧物料

---

### 📍 **阶段3: 完整抓取-放置流程** (预计1-2天)

#### 3.1 测试完整流程
运行测试用例:
```c
Test_Vision_PickAndPlace_Red();  // 抓取并放入物料盘
```

#### 3.2 参数优化
根据实际测试调整:
1. **等待时间:** HAL_Delay() 的延时可能需要增减
2. **速度档位:** 视觉引导时建议用精确模式 (`Motor_Set_Speed_Profile(0)`)
3. **抓取高度:** 根据物料厚度调整 `HEIGHT_PICKUP_LOWER`

**验证点:**
- [ ] 能稳定抓取红色物料
- [ ] 能准确放入物料盘1 (185°位置)
- [ ] 整个流程无卡顿或碰撞

---

### 📍 **阶段4: 多物料自动分拣** (预计2-3天)

#### 4.1 三色分拣测试
运行测试用例:
```c
Test_Vision_Auto_Sorting();  // 红→绿→蓝自动分拣
```

#### 4.2 错误处理优化
添加容错机制:
```c
// 示例:如果识别失败,重试3次
for (int retry = 0; retry < 3; retry++) {
    if (Task_Vision_Pickup(COLOR_RED)) break;
    HAL_Delay(500);  // 失败后等待再试
}
```

#### 4.3 二维码任务集成
运行测试用例:
```c
Test_Vision_QRCode();  // 读取二维码
```

**验证点:**
- [ ] 能连续抓取3种颜色物料
- [ ] 每种物料放入正确的物料盘
- [ ] 二维码能稳定识别并返回正确数据

---

## 📊 测试建议

### 分步测试顺序 (从易到难)
1. ✅ **已验证:** 纯机械爪测试 (无视觉)
2. ⏭️ **下一步:** 视觉通信测试 (`Test_Vision_Communication`)
3. ⏭️ 视觉引导定位测试 (`Test_Vision_Positioning`)
4. ⏭️ 单物料抓取测试 (`Test_Vision_Pickup_Red`)
5. ⏭️ 完整流程测试 (`Test_Vision_PickAndPlace_Red`)
6. ⏭️ 三色自动分拣 (`Test_Vision_Auto_Sorting`)
7. ⏭️ 二维码识别 (`Test_Vision_QRCode`)

### 调试工具推荐
1. **串口调试助手:** 监控UART3收发数据 (推荐SSCOM)
2. **OLED显示:** 实时显示视觉坐标和任务状态
3. **逻辑分析仪:** 排查UART时序问题 (如果有)

---

## 🔧 常见问题预案

### 问题1: 视觉无响应
**原因:** LubanCat服务未启动或UART接线错误
**解决:** 
```bash
# SSH登录LubanCat后执行:
sudo systemctl start vision.service
sudo systemctl status vision.service
```

### 问题2: 定位偏差大
**原因:** 相机坐标系未标定
**解决:** 调整转换系数 K_X 和 K_Y (建议从0.3~0.8范围尝试)

### 问题3: 抓取失败
**原因:** 高度不匹配或夹持力不够
**解决:** 
- 调整 `HEIGHT_PICKUP_LOWER` (建议±2mm微调)
- 增加 `Gripper_Claw_Close()` 后的延时

### 问题4: 物料放歪
**原因:** 云台角度误差或物料盘位置偏移
**解决:** 重新校准 `PLATE_ANGLES` 数组中的角度值

---

## 📝 后续扩展方向

### 可选优化 (时间充裕时)
1. **PID闭环控制:** 底盘移动时引入视觉反馈,实时修正偏差
2. **多目标规划:** 优化抓取顺序,减少移动距离
3. **异常恢复:** 识别失败时自动调整位置重试
4. **路径规划:** 集成A*算法避开障碍物

### 比赛任务集成
根据附件1的比赛文档:
1. **启停区→物料台:** 视觉引导取料
2. **物料台→测试区:** 按二维码指示放置
3. **凸台识别:** 使用 `Visual_Send_Platform_Request()`
4. **二维码任务:** 使用 `Task_Vision_ReadQRCode()`

---

## 🎓 学习建议

### 关键代码理解
1. **视觉数据结构:** `VisualRxData_t` 定义了所有识别结果
2. **任务码定义:** `VisualTask_t` 枚举了所有视觉任务类型
3. **坐标转换:** 像素坐标→底盘移动距离 (需要标定)

### 推荐阅读
- `visual_comm.c`: 理解UART协议封装
- `task_vision_gripper.c`: 任务流程实现细节
- 比赛文档附件1: 了解完整任务要求

---

## ✅ 下一步行动清单

**立即可做:**
1. [ ] 在 `main.c` 中启用视觉初始化代码
2. [ ] 运行 `Test_Vision_Communication()` 验证通信
3. [ ] 检查LubanCat上视觉服务是否正常

**后续任务:**
4. [ ] 标定相机坐标系 (K_X, K_Y)
5. [ ] 测试单物料识别与抓取
6. [ ] 完成三色自动分拣
7. [ ] 集成二维码任务

---

**预计总开发时间:** 6-10天 (视调试进度而定)

祝开发顺利! 🚀
